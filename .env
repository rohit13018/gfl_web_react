# API integration — safe to commit (no secrets; these values ship in the client bundle).
# Copy to .env.local to override locally.
VITE_API_BASE_URL=https://8kfas3uoje.execute-api.ap-south-1.amazonaws.com
VITE_WEB_ADMIN_AUTH_MODE=admin
VITE_API_TIMEOUT_MS=20000
